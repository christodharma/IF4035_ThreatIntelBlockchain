{
  "name": "hardhat-project",
  "scripts": {
    "start": "npx hardhat run main.ts --network localhost",
    "deploy": "npx hardhat compile && npx hardhat ignition deploy ignition/modules/Oracle.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7"
  }
}
